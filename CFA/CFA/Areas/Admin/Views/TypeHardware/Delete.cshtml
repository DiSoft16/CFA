@model CFA.Models.TypeHardware

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<div class="panel panel-danger">
    <div class="panel-heading">
        <h3>Are you sure you want to delete this?</h3>
        <h4>TypeHardware</h4>
    </div>

    <div class="list-group">
        <a class="list-group-item">
            <h4 class="list-group-item-heading">@Html.DisplayNameFor(model => model.name)</h4>
            <p class="list-group-item-text">@Html.DisplayFor(model => model.name)</p>
        </a>
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="col-sm-1">
            <input type="submit" class="btn btn-danger edit-button" value="Delete" />
        </div>
        <div class="col-sm-1">
            <a href="/TypeHardware/Index" class="btn btn-primary edit-button">Back</a>
        </div>
    }
</div>
